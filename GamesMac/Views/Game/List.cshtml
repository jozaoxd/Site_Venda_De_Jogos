@model GameListViewModel

<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
<div class="row">
@foreach(var game in Model.Games)
        {
            <partial name ="_GamesResumo" for ="@game"/>
        }
    </div>
</div>